import { shallowMount } from '@vue/test-utils'
import SmartCalendar from '@/components/SmartCalendar.vue'
import { test } from 'vitest'

describe('TESTING SMARTCALENDAR.VUE', () => {
  test.todo('should render the Day calendar when selected')
  test.todo('should render the Week calendar when selected')
  test.todo('should render the Month calendar when selected')
})
